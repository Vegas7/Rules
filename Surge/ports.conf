# STUN
PROTOCOL,STUN
AND,((DEST-PORT,3478),(PROTOCOL,UDP))

# Tailscale
AND,((DEST-PORT,40000),(PROTOCOL,UDP))
AND,((DEST-PORT,41641),(PROTOCOL,UDP))

# EasyTier
DEST-PORT,11211
DEST-PORT,22020
DEST-PORT,11010-11013
DEST-PORT,11020-11023

# Steam
AND,((DEST-PORT,27000-27100),(PROTOCOL,UDP))
AND,((DEST-PORT,27036),(PROTOCOL,TCP))

# RustDesk
AND,((DEST-PORT,21114-21119),(PROTOCOL,TCP))
AND,((DEST-PORT,21116),(PROTOCOL,UDP))

# Plex
AND,((DEST-PORT,32400), (PROTOCOL,TCP))

# Windows Delivery Optimization
AND,((DEST-PORT,7680), (PROTOCOL,TCP))

# Time Sync
AND,((PROTOCOL,UDP),(DEST-PORT,123))
